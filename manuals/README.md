# Manuals

Drop your Vet-eye USG PDF manuals / specs / FAQ documents in this folder, then
run the ingestion script from the project root:

```bash
python ingest.py
```

PDFs here are git-ignored (see `.gitignore`). Scanned/image-only PDFs won't
extract text — run OCR on them first.
